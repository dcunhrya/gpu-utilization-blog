---
title: "GPU Bottleneck"
order: 3
description: "Identifying what limits GPU utilization in your pipeline."
---

### CPU-GPU Bottleneck

Now that we have covered CPUs and GPUs, let’s look at how the bottleneck occurs and then what we can do to fix it. We said the GPU has thousands of cores ready to parallelize operations, but it needs the CPU to hand off the tasks. 
When you train a model, your GPU cannot read directly from your SSD. The CPU has to load the raw data, decode it, apply augmentations, batch it, and hand it off. 
If your CPU takes 50 milliseconds to prepare a batch, and your GPU only takes 10 milliseconds to compute the forward and backward passes, your GPU spends 40 milliseconds idling.

### Roofline Model

This problem is formalized as the Roofline Model. It measures the performance (FLOPs/second) against arithmetic intensity (FLOPs/byte) where FLOPs are floating-point operations per second. If your arithmetic intensity is low (you load a massive amount of data but do very little math with it), you hit the slanted "Memory-Bound" roof. If your arithmetic intensity is high (you load a small amount of data but do a massive amount of matrix multiplication with it), you hit the flat "Compute-Bound" roof.

For most research experiments, the GPU parallelism is rarely ever the bottleneck. Whether you are constrained by the CPU struggling to parse data, the PCIe bus clogging up, or the VRAM bandwidth limits, it is in the memory-bound regime. The key to this is almost always faster data movement.

### Visualizing the Roofline Model

<RooflineModel />
